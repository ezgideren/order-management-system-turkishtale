import { UserRole } from '@/types';
export declare const canManageOrders: (role: UserRole) => boolean;
export declare const canAccessKitchen: (role: UserRole) => boolean;
export declare const canManageMenu: (role: UserRole) => boolean;
